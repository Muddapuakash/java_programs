package com.java.opps.package1;

public class Parent {
	
	protected int var = 10;
	
	/*Java Supports 4 Differents access levels
	 * 1.Public  - can be accessed from
	 *            a.within the same class;
	 *            b.from other class in the same package
	 *            c.from other class in a diffrent package
	 *2.private - can be accessed only from the class in which it is declared
	 *3.Protected - a.within the same class;
	 *            b.from other class in the same package
	 *            c.from other class in a diffrent package
	 *4.Default - 1)with same class,same package level modeifier
	 *  
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * */

}
